#include "main.h"

/** A funtion that prints n times table
 * starting from 0
 * if n is less than 15 or 0, the would not print anything
 */

int main(void)
{
	print_times_table(3);
	_putchar('\n');
	print_times_table(5);
	_putchar('\n');
	print_times_table(98);
	_putchar('\n');
	print_times_table(12);
	return (0);
}
