#include "main.h"

/**
 * main - a function that reverses the content
 * of an array of integers
 * @n: the number of integers to swap
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Courage TS";
	print_rev(str);
	return (0);
}
